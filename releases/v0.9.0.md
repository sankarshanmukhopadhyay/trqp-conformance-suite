# v0.9.0 — Operational Stack Baseline

This release aligns TRQP Conformance Suite with the Operational Stack baseline.

## Highlights

- Add `profiles/interop_demo.yaml` for low-friction workshops and demos.
- Add shared `--run-id` and `--target-id` support in `cts/run.py`.
- Emit `cts-report.json` for direct consumption by the TRQP Assurance Hub.
