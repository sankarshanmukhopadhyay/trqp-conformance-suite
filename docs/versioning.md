# Versioning

SemVer: `MAJOR.MINOR.PATCH`

- MAJOR: incompatible changes to profiles, requirement IDs, evidence formats
- MINOR: additive profiles/requirements/tests; backward compatible
- PATCH: fixes/clarifications; no requirement semantic changes

If semantics must change, introduce a new requirement ID and deprecate the old one (do not silently alter).
